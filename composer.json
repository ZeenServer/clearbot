{
  "name": "acme/telegram-modbot",
  "description": "Telegram moderation bot (PHP 8.3, MySQL + Redis) with pattern filter, local/global bans and logging",
  "type": "project",
  "require": {
    "php": "^8.3",
    "vlucas/phpdotenv": "^5.6",
    "ext-curl": "*",
    "ext-json": "*",
    "ext-redis": "*",
    "ext-pdo": "*"
  },
  "autoload": {
    "psr-4": {
      "App\\": "src/"
    }
  }
}
